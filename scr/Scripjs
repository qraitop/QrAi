const chat = document.getElementById("chat");
const input = document.getElementById("prompt");
const button = document.getElementById("send");

// ⚠️ Deploy hone ke baad is URL ko apne Worker URL se replace karna.
// Example:
// https://qraitop.<your-subdomain>.workers.dev
const API_URL = "https://YOUR-WORKER.workers.dev";

function addMessage(text, type) {
    const div = document.createElement("div");
    div.className = "message " + type;
    div.textContent = text;
    chat.appendChild(div);
    chat.scrollTop = chat.scrollHeight;
}

async function sendMessage() {

    const message = input.value.trim();

    if (!message) return;

    addMessage(message, "user");

    input.value = "";

    addMessage("Thinking...", "ai");

    try {

        const res = await fetch(API_URL, {
            method: "POST",
            headers: {
                "Content-Type": "application/json"
            },
            body: JSON.stringify({
                message
            })
        });

        const data = await res.json();

        chat.lastChild.remove();

        if (data.success) {
            addMessage(data.reply, "ai");
        } else {
            addMessage("Error: " + data.error, "ai");
        }

    } catch (err) {

        chat.lastChild.remove();

        addMessage(
            "Connection failed. Check Worker URL.",
            "ai"
        );

    }

}

button.onclick = sendMessage;

input.addEventListener("keydown", e => {

    if (e.key === "Enter" && !e.shiftKey) {

        e.preventDefault();

        sendMessage();

    }

});
qrai-top/
│
├── package.json
├── wrangler.jsonc
├── src/
│   └── index.js
│
└── public/
    ├── index.html
    ├── style.css
    └── script.js
const AQ.Ab8RN6JDCchAdTIR9_JWC5DzzlNHqfVP1ZP1GIrTLOcYHq22gg = "https://qraitop.qrai-top.workers.dev";
